const mongoose = require('mongoose'); require('dotenv').config({ path: '.env.local' }); mongoose.connect(process.env.MONGODB_URI).then(async () => { const db = mongoose.connection.db; const pools = await db.collection('questions').distinct('pool'); console.log('Pools:', pools); const categories = await db.collection('questions').distinct('category'); console.log('Categories:', categories); process.exit(0); });